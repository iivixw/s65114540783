# --- API Configuration ---
# กำหนดสภาพแวดล้อมและพอร์ตสำหรับ API
NODE_ENV=production
PORT=10783

# --- Database Configuration ---
# กำหนดค่าเชื่อมต่อสำหรับฐานข้อมูล PostgreSQL
DB_HOST=postgres
DB_PORT=20783
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass
POSTGRES_DB=appdb

# --- Redis Configuration ---
# กำหนดค่าเชื่อมต่อสำหรับ Redis
REDIS_HOST=redis
REDIS_PORT=30783

# --- Frontend CORS Origin ---
# อนุญาตให้ Frontend ที่รันบน IP และพอร์ตนี้สามารถเรียกใช้ API ได้
CORS_ORIGIN=http://10.80.29.16:10883

